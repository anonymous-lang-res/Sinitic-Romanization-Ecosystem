The HTML files must be placed in the same directory as the `libs` folder.
`zh` indicates that the interface text is in Chinese only.
`bilingual` indicates that the interface text is provided in both Chinese and English.
